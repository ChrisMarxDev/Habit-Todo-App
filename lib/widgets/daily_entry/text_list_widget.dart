import 'package:flutter/material.dart';

class TextListWidget extends StatelessWidget {
  const TextListWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
